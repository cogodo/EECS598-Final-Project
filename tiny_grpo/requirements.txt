# Simple, flexible requirements - works with any recent versions!
# Just install what's missing, keep what you have

# Core training (minimum versions - your newer versions work great!)
torch>=2.0.0
transformers>=4.40.0
peft>=0.7.0              # LoRA adapters (small checkpoints!)

# Utilities
tqdm>=4.65.0             # progress bars
pandas>=2.0.0            # data handling
datasets>=2.14.0         # for downloading GSM8K

# Optional
wandb>=0.15.0            # experiment tracking (optional)
matplotlib>=3.7.0        # plotting (optional)
seaborn>=0.12.0          # pretty charts (optional)

# ✓ Works with your current env:
#   torch==2.6.0 ✓
#   transformers==4.53.2 ✓
#   wandb==0.22.2 ✓
#
# Just install: pip install peft tqdm pandas datasets matplotlib seaborn
