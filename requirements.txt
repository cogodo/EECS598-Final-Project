# Minimal requirements for Google Colab
# Colab already has: torch, numpy, pandas, tqdm, requests, etc.

# Core ML packages
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.25.0
datasets>=2.14.0

# Math verification (used by MathVerifier)
verl>=0.6.0

# Logging (optional - can set wandb.init(mode="disabled"))
wandb>=0.15.0
